{
  "phases": {
    "setup": {
      "nixPkgs": ["nodejs_20", "yarn", "openssl"]
    },
    "install": {
      "cmds": ["yarn install --frozen-lockfile"]
    },
    "build": {
      "cmds": ["npx prisma generate", "yarn build"]
    }
  },
  "start": {
    "cmd": "yarn start"
  }
}
