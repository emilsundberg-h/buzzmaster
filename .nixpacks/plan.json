{
  "phases": {
    "setup": {
      "nixPkgs": ["nodejs-20_x", "yarn-1_x", "openssl"]
    },
    "install": {
      "cmds": ["yarn install --frozen-lockfile"]
    },
    "build": {
      "cmds": ["npx prisma generate", "yarn build"]
    }
  },
  "start": {
    "cmd": "yarn start"
  }
}
